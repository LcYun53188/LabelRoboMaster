# LabelMaster Web UI 改造计划

本文档旨在详细规划将现有的 `LabelMaster` Qt 桌面应用重构为现代化Web应用的具体方案、技术路线和功能需求。

## 1. 项目目标

将 `LabelMaster` 从一个依赖特定操作系统和本地环境的Qt桌面程序，转变为一个通过浏览器即可访问、跨平台的Web应用。新应用将完整复刻原版的核心标注功能，同时提供更现代化的用户界面和更便捷的访问体验。

## 2. 技术选型 (Technology Stack)

| 类别             | 技术/库                 | 理由                                                                                                     |
| ---------------- | ----------------------- | -------------------------------------------------------------------------------------------------------- |
| **前端框架**     | **React 18+** (with TypeScript) | 用于构建高性能、组件化的用户界面。TypeScript提供类型安全，增强代码健壮性。                               |
| **UI 组件库**   | **MUI v5** (Material-UI)  | 提供一套丰富、美观且开箱即用的React组件，可快速搭建出符合Material Design风格的专业界面。                |
| **核心绘图/画布** | **Konva.js**            | 强大的HTML5 Canvas库，专门用于处理图形对象。完美契合需求，用于在图片上绘制、移动和操作四点标注框。 |
| **后端服务**     | **Node.js + Express**   | 轻量、高效的JavaScript运行时和Web框架，用于处理文件系统操作（读写图片和标签），并为前端提供REST API。    |
| **状态管理**     | **Zustand**             | 一个轻量、简洁的React状态管理库，能有效管理散布在不同组件间的复杂状态，如当前图片、标签列表等。       |
| **构建/开发工具**  | **Vite**                | 提供极速的开发服务器和高效的项目打包能力，显著提升开发体验。                                               |

## 3. 系统架构

### 3.1. 后端 (Backend)

后端是一个基于 `Node.js` 和 `Express` 的简单HTTP服务器，其核心职责是作为前端和服务器本地文件系统之间的桥梁。

**定义的REST API接口:**

*   `POST /api/select-directory`: 接收前端传来的本地文件夹路径，并设置为当前工作目录。
*   `GET /api/images`: 读取并返回当前工作目录下的所有图片文件列表（例如 `['1.png', '2.jpg']`）。
*   `GET /api/image/:filename`: 根据文件名，读取并返回图片文件的二进制数据。
*   `GET /api/label/:filename`: 根据图片文件名，读取并返回对应的 `.txt` 标签文件内容。
*   `POST /api/label/:filename`: 接收前端发送的JSON格式标签数据，并将其转换格式后写入到对应的`.txt`文件中，实现保存功能。

### 3.2. 前端 (Frontend)

前端是一个单页应用 (SPA)，负责所有的UI渲染和用户交互。

**核心React组件:**

*   `<App>`: 根组件，负责整体页面布局和路由。
*   `<FileBrowserPanel>`: 左侧文件浏览器，通过调用 `/api/images` API获取并展示图片列表。
*   `<AnnotationWorkspace>`: 中心工作区，包含以下子组件：
    *   `<Toolbar>`: 顶部工具栏，放置各类操作按钮。
    *   `<CanvasViewer>`: 核心画布组件，使用`Konva.js`渲染背景图片和标注形状，并处理所有绘图、拖拽、缩放、平移等交互。
*   `<LabelPanel>`: 右侧标签面板，展示当前图片已有的标注目标列表，并提供编辑和删除功能。

## 4. UI/UX 布局设计

采用经典的三栏式仪表盘布局：

*   **左侧抽屉 (File Browser)**: 可收缩。显示图片文件列表，高亮显示当前文件。
*   **中心区域 (Workspace)**:
    *   **顶部**: 紧凑的工具栏。
    *   **画布**: 图片背景 + `Konva.js` 绘图层，是用户主要交互区域。
*   **右侧抽屉 (Label Panel)**: 可收缩。显示当前图片中的标签列表，并可对选中的标签进行属性编辑。

## 5. 核心功能实现清单

以下是从原版`LabelMaster`继承的核心功能，需要在此次Web版中重新实现：

- [ ] **1. 基础框架**
    - [ ]  搭建Vite + React + TS项目。
    - [ ]  实现基础的Node.js/Express后端服务。
    - [ ]  使用MUI搭建三栏式UI布局框架。
- [ ] **2. 文件与数据显示**
    - [ ]  实现后端API：选择目录、获取图片列表、获取图片文件、获取标签文件。
    - [ ]  前端能够调用API，在左侧面板显示图片列表。
    - [ ]  点击文件列表中的项，可以在中心区域显示对应图片。
- [ ] **3. 核心标注功能**
    - [ ]  在`Konva.js`画布上，实现添加新的四点标注框（Add Target）。
    - [ ]  标注框的四个顶点可以被鼠标拖动修改。
    - [ ]  整个标注框可以被选中和整体移动。
    *   [ ]  支持画布的滚轮缩放和拖拽平移。
- [ ] **4. 标签管理与保存**
    - [ ]  在右侧面板显示当前图片的所有标注框列表。
    - [ ]  双击或通过按钮可以修改选中标注框的类别（颜色、ID）。
    - [ ]  实现删除选中的标注框功能。
    - [ ]  实现 "保存" 功能，将当前所有标注框数据通过API发送给后端，并由后端写入`.txt`文件。
- [ ] **5. 导航与辅助功能**
    - [ ]  实现 "上一张" (Q) / "下一张" (E) 图片的快速切换功能（切换时应触发自动保存）。
    - [ ]  实现 "模式选择" (Mode) 下拉菜单。
    - [ ]  (可选) 实现图片亮度增强、直方图均衡化等视图增强功能。

## 6. 开发路线图 (Roadmap)

1.  **第一阶段：环境搭建与静态布局**
    *   初始化前端和后端项目。
    *   完成基本的MUI组件布局，实现页面的静态框架。
2.  **第二阶段：实现数据通道**
    *   开发所有必需的后端API接口。
    *   在前端实现API的调用，打通前后端数据链路，能够加载并显示图片。
3.  **第三阶段：核心标注功能开发**
    *   集中精力开发 `<CanvasViewer>` 组件，利用 `Konva.js` 实现所有绘图和交互逻辑。
4.  **第四阶段：完整工作流闭环**
    *   开发标签管理功能和保存功能，形成从“加载图片 -> 标注 -> 保存标签”的完整工作流。
5.  **第五阶段：辅助功能与优化**
    *   实现快捷键、图片导航和其他辅助功能。
    *   进行整体测试、Bug修复和体验优化。
